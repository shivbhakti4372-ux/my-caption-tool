// 1. Dark Mode Background + Center Screen Placement (UI Layout)
<div className="min-h-screen bg-gray-900 flex items-center justify-center p-4">

  // 2. Glowing Glassmorphism Card (UI Container)
  <div className="bg-gray-800 rounded-2xl shadow-[0_0_50px_rgba(59,130,246,0.3)] border border-gray-700">

    // 3. Gradient Color Text (UI Typography)
    <h1 className="text-3xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-purple-500">
      Viral Caption Generator
    </h1>

    // 4. Stylish Input Field (UI Input)
    <textarea className="bg-gray-900 border border-gray-600 rounded-xl focus:ring-2 focus:ring-blue-500" />

    // 5. Animated Clickable Button with Scale Effect (UI Interactive Element)
    <button className="bg-gradient-to-r from-blue-600 to-purple-600 hover:scale-105 active:scale-95">
      ✨ Generate Magic ✨
    </button>

  </div>
</div>
